# 🔍 问题排查清单

## 📊 统计卡片不显示？

### 步骤 1：验证用户名
- [ ] 确认你的 GitHub 用户名是 `HarmmerRay`（区分大小写）
- [ ] 访问测试链接：https://github-readme-stats.vercel.app/api?username=HarmmerRay
- [ ] 如果测试链接能显示，说明服务正常，问题在 README 配置

### 步骤 2：检查 README 配置
- [ ] 确认 README.md 中的用户名拼写正确
- [ ] 确认使用的是正确的 URL 格式
- [ ] 尝试在浏览器中直接打开图片链接

### 步骤 3：检查仓库
- [ ] 确保至少有一些公开的仓库
- [ ] 如果所有仓库都是私有的，统计卡片可能显示为空

### 步骤 4：清除缓存
在 URL 后添加 `&cache_seconds=0` 强制刷新：
```
https://github-readme-stats.vercel.app/api?username=HarmmerRay&cache_seconds=0
```

---

## 🐍 贪吃蛇不显示？

### 步骤 1：检查同名仓库
- [ ] 确认已创建名为 `HarmmerRay` 的公开仓库
- [ ] 仓库名必须与 GitHub 用户名完全一致（区分大小写）
- [ ] 仓库必须是 Public（公开）

### 步骤 2：检查 GitHub Actions 权限
1. 进入 `HarmmerRay` 仓库
2. 点击 Settings（设置）
3. 点击左侧的 Actions → General
4. 检查以下设置：
   - [ ] "Workflow permissions" 设置为 "Read and write permissions"
   - [ ] 勾选 "Allow GitHub Actions to create and approve pull requests"

### 步骤 3：检查工作流文件
- [ ] 确认存在 `.github/workflows/snake.yml` 文件
- [ ] 文件内容是否正确（参考 `github-stats-setup.md`）

### 步骤 4：手动触发工作流
1. 进入 `HarmmerRay` 仓库
2. 点击 Actions 标签页
3. 选择 "Generate Snake Animation" 工作流
4. 点击 "Run workflow" 按钮
5. 等待运行完成（通常需要 1-2 分钟）

### 步骤 5：检查工作流运行结果
- [ ] 查看 Actions 标签页，确认工作流运行成功（绿色 ✓）
- [ ] 如果有错误（红色 ✗），点击查看详细日志
- [ ] 常见错误：
  - 权限不足：检查步骤 2 的权限设置
  - 文件路径错误：检查工作流配置

### 步骤 6：检查 output 分支
- [ ] 工作流成功后，应该会创建 `output` 分支
- [ ] 切换到 `output` 分支
- [ ] 确认存在 `github-contribution-grid-snake.svg` 文件

### 步骤 7：验证文件可访问
直接访问以下链接：
```
https://raw.githubusercontent.com/HarmmerRay/HarmmerRay/output/github-contribution-grid-snake.svg
```

- [ ] 如果显示 404：文件还未生成，返回步骤 4
- [ ] 如果显示 SVG 内容：文件已生成，问题可能在 README 引用

### 步骤 8：检查 README 引用
- [ ] 确认 README.md 中的路径正确
- [ ] 确认使用的是 `raw.githubusercontent.com` 而不是 `github.com`
- [ ] 等待几分钟让 GitHub 缓存更新

---

## 🚀 快速修复命令

如果工作流文件不存在，可以在同名仓库中执行：

```bash
# 创建目录
mkdir -p .github/workflows

# 创建工作流文件（需要手动复制内容）
# 参考 github-stats-setup.md 中的配置
```

---

## 📞 仍然无法解决？

1. **检查 GitHub 状态**：访问 https://www.githubstatus.com/ 确认服务正常
2. **查看工作流日志**：Actions 标签页中有详细的错误信息
3. **参考官方文档**：
   - [github-readme-stats](https://github.com/anuraghazra/github-readme-stats)
   - [Platane/snk](https://github.com/Platane/snk)

